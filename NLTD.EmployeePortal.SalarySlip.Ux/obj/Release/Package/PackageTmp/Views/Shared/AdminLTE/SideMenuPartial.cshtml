<aside class="main-sidebar">
    <!-- sidebar: style can be found in sidebar.less -->
    <section class="sidebar">
        <!-- Sidebar user panel (optional) -->
        <div class="user-panel">
            @*<div class="pull-left image">
                <img src="@ViewBag.Avatar" class="img-circle" alt="User Image" style="width:160px;">
            </div>*@
            @*<div class="pull-left info">
                <p>@Html.Raw((String)ViewBag.DisplayName)</p>
            </div>*@
        </div>
        @{
            if (ViewBag.MenuText == "NoProfile")
            {
                <div class="alert alert-danger">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">                        
                    </button>
                    Profile not found. Contact website admin.
                </div>
            }
            else
            {
                @Html.Raw((String)ViewBag.MenuText)
            }


        }
        
    </section>
    <!-- /.sidebar -->
</aside>
